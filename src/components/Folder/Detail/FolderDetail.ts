import { defineComponent} from "vue";
import LocationList from "../../Location/List/index.vue";

export default defineComponent({
    name: 'FolderDetail',
    components: {
        LocationList
    },
    setup() {

        return {
        }
    }
});
